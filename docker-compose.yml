
services:
  item-db:
    image: mongo:3
    volumes:
      - app-db:/data/db
    restart: always # rekomendasi
  
  item-app:
    image: edgartanamal04/item-app:v1
    ports:
      - "80:8080"
    depends_on:
      - item-db
    restart: always # rekomendasi

volumes:
  app-db:
